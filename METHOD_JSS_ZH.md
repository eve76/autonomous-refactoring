# 3 研究方法

## 3.1 研究设计

本研究旨在考察一个协同式多智能体系统能否在保持软件外部行为不变的前提下，自主改善既有软件系统的结构质量，并对大语言模型推理所产生的计算开销进行控制。为此，本研究将自动重构定义为一个带约束的迭代优化问题。在每轮迭代中，一个或多个智能体首先识别潜在的代码质量问题，随后实施保持行为不变的重构，并将候选修改提交给确定性的验收程序。候选修改只有在降低预先定义的静态质量目标函数，并同时通过目标项目原生的构建与正确性测试后，才会被系统接受。

该方法在设计上将基于模型的判断与确定性控制相分离。需要结合上下文进行判断的活动由大语言模型智能体承担，包括选择重构机会、理解源代码、提出结构调整方案，以及判断某个智能体是否已陷入无效工作。共享状态更新、指标计算、作用域约束、版本控制操作、候选验收、停止条件和实验数据记录则由普通 Python 程序完成。通过这种分工，任何智能体都不能仅凭自身生成的文字声明某项修改已经成功，而必须提供可由系统独立验证的证据。

系统实现了一种多智能体配置，由一个编排智能体、三个分析智能体和三个编程智能体组成。分析智能体和编程智能体的数量可以通过配置调整，但本节将默认配置作为系统的参考实现。当前系统支持 C/C++ 和 Go 项目，并为 MongoDB Query 与 FerretDB 提供锁定的执行配置。当前实现不包含单智能体对照系统，运行时性能测量也尚未被纳入候选修改的合并条件。

## 3.2 问题形式化

设 \(S_i\) 表示第 \(i\) 次成功集成后目标仓库的已接受状态，其中 \(S_0\) 表示固定的基线版本。每个状态由五个静态质量维度描述：圈复杂度、认知复杂度、逻辑代码行数、函数参数个数和重复代码行比例。前四项指标以函数为单位测量，重复代码则在完整的生产代码范围内测量。

系统的目标是寻找一系列保持行为不变的状态变换：

\[
S_0 \rightarrow S_1 \rightarrow \cdots \rightarrow S_n，
\]

并要求每一个被接受的状态转换满足：

\[
F(S_{i+1}) < F(S_i)，
\]

其中，\(F\) 是第 3.4 节定义的加权静态质量惩罚函数。此外，每一个被接受的状态都必须通过目标仓库的构建与正确性测试。因此，候选状态的验收关系可以表示为：

\[
\operatorname{accept}(S_i,S_{i+1}) =
\begin{cases}
1, & F(S_{i+1}) < F(S_i) \land B(S_{i+1}) \land T(S_{i+1})，\\
0, & \text{其他情况}，
\end{cases}
\]

其中，\(B\) 和 \(T\) 分别表示构建成功和测试成功。该定义要求候选状态的惩罚值严格下降。即使某项修改改善了目标函数未覆盖的性质，只要其总惩罚没有下降，当前系统也不会接受该修改，因为实验无法根据预先定义的测量目标确认这种收益。

优化过程还受到源代码范围的约束。系统可以为了满足传递构建依赖而提供完整仓库工作树，但静态测量和修改权限仍然限定在配置的目标子目录内。测试源代码不参与质量指标计算，智能体也不得通过修改测试来使候选修改通过验收。

## 3.3 系统架构

系统由确定性的协调层和基于大语言模型的智能体层组成。协调层在主线程中运行，并拥有全部共享状态。分析智能体和编程智能体分别通过两个线程池启动为独立的命令行子进程；编排智能体不是常驻进程，而只在系统需要作出协调决策时被同步调用。

表 1 概括了系统的主要组成及其职责。

**表 1  系统主要组成及职责**

| 组成 | 默认数量 | 执行方式 | 主要职责 |
|---|---:|---|---|
| 协调器 | 1 | Python 主线程 | 管理共享状态、调度工作、执行停止规则并生成实验产物 |
| 编排智能体 | 1 | 同步模型接口调用 | 分配任务并评估可能陷入停滞的编程智能体 |
| 分析智能体 | 3 | 独立命令行子进程 | 检查已接受代码并报告可执行的重构机会 |
| 编程智能体 | 3 | 独立命令行子进程 | 在隔离的 Git 工作树中实施被分配的重构任务 |
| 合并门禁 | 按需创建 | 独立 Python 进程 | 评估、测试并集成已经提交的候选修改 |
| 静态分析层 | 按需运行 | 确定性外部工具 | 测量代码质量并计算优化目标 |

系统首先对配置的生产代码范围执行基线测量。当产品待办列表中没有可执行问题时，编排智能体会调度分析智能体检查高惩罚函数。经过确认的问题被写入持久化的产品待办列表，随后由编排智能体分配给空闲的编程智能体。每个编程智能体在隔离的特性分支中修改代码，并调用合并门禁验证候选修改。若门禁验证成功，本次运行的集成分支将向前推进，协调器随后重新测量完整目标范围。更新后的测量结果将作为后续问题发现和任务调度的依据。

分析智能体和编程智能体通过线程安全的消息队列向协调器发送结果。智能体不直接写入产品待办列表或全局运行状态。协调器在单写者循环中依次处理消息，并使用临时文件与原子替换方式持久化共享状态。这一设计在允许多个模型子进程异步运行的同时，避免了对共享 JSON 文件的并发修改。

## 3.4 质量目标函数

### 3.4.1 指标选择

质量目标函数由表 2 所示的五项指标组成。所有阈值均可配置，表中数值为系统默认值。

**表 2  静态质量指标及默认参数**

| 指标 | 符号 | 默认阈值 | 测量单位 | 测量工具 |
|---|---|---:|---|---|
| 圈复杂度 | CCN | 15 | 每个函数 | Lizard |
| 认知复杂度 | Cog | 15 | 每个函数 | C/C++ 使用认知复杂度工具；Go 使用 gocognit |
| 逻辑代码行数 | NLOC | 30 | 每个函数 | Lizard |
| 参数个数 | Param | 5 | 每个函数 | Lizard |
| 重复代码行比例 | Dup | 0 | 整个目标范围 | C/C++ 使用 Duplo；Go 使用 mibk/dupl |

所有适用工具均使用同一生产文件集合。系统根据可配置的路径组成规则排除测试目录，并依据语言命名约定排除同目录下的测试源文件。统一的文件选择过程可以防止函数级指标与重复代码比例针对不同的源代码范围进行测量。

### 3.4.2 函数级惩罚

对于函数级指标 \(m\)，设 \(x_{f,m}\) 为函数 \(f\) 的测量值，\(T_m\) 为相应阈值。该函数对总目标的惩罚贡献定义为：

\[
p_m(x_{f,m}) = 100\left(1-\frac{T_m}{\max(T_m,x_{f,m})}\right)。
\]

当指标值不超过阈值时，惩罚为零；当指标值超过阈值时，惩罚随超标程度单调增加，并渐近于 100。因此，即使一次重构尚未将极端函数完全降至阈值以内，只要降低了其超标程度，系统仍然能够观察到连续的质量改进；当函数最终回到阈值以内时，其对应惩罚将被完全消除。

### 3.4.3 重复代码惩罚

设 \(r\in[0,1]\) 表示生产代码中的重复行比例。由于当前目标函数将任意非零重复代码都视为待改善对象，重复代码惩罚定义为：

\[
p_{dup}(r) = 100\frac{r}{r+0.1}。
\]

实现中使用比例值而不是百分数表示 \(r\)。C/C++ 适配器从 Duplo 的摘要中取得重复行数和过滤后的总行数。Go 适配器则规范化正反方向重复对，并在计算比例前合并同一文件内相互重叠的物理行区间，从而防止重复计数并确保结果位于 0 到 1 之间。

### 3.4.4 综合目标与实验选择变量

状态 \(S\) 的总惩罚定义为：

\[
F(S) =
\sum_{m \in M_f} w_m \sum_{f \in \mathcal{F}_m} p_m(x_{f,m})
+ w_{dup}p_{dup}(r)，
\]

其中，\(M_f=\{CCN,Cog,NLOC,Param\}\)，\(\mathcal{F}_m\) 是相应分析工具为指标 \(m\) 报告的函数集合，\(w_m\) 是该指标的配置权重。Lizard 与认知复杂度工具产生的函数记录被保留为两个独立集合，从而避免因不同分析工具对函数的识别结果不完全一致而丢失有效惩罚。

完整配置下五个指标的权重均为 1。将某一权重设为 0 会使该指标完全退出验收目标，因此可以在不修改合并门禁的情况下构造单指标实验和指标消融实验。虽然实现允许使用任意实数权重，但受控实验应预先定义权重方案，并在每次运行的结果中完整记录。

### 3.4.5 分布性结果指标

总惩罚为优化过程提供单一反馈信号，但仅凭这一数值无法解释代码质量变化发生在哪些位置。因此，系统同时记录每项函数级指标在基线和最终状态下的完整分布，包括函数数量、均值、中位数、第 90、第 95 和第 99 百分位数、最大值、超阈值函数数量，以及是否已经清除全部违规。重复代码则记录比例、百分比、重复行数、总行数、重复块数量和是否清零。

目标函数与分布统计承担不同作用：前者决定候选修改能否被接受，后者用于判断质量改善是覆盖了较广泛的函数群体，还是仅集中在少量极端函数上。

## 3.5 静态分析过程

每次测量开始时，分析层首先在配置的目标范围内枚举与目标语言匹配的生产源文件。Lizard 以 CSV 模式运行，为每个函数生成一条 CCN、NLOC 和参数个数记录。系统不解析 Lizard 的默认表格输出，因为默认报告可能在警告区域再次列出高复杂度函数，从而恰好放大对目标函数贡献最大的观测值。

认知复杂度由独立工具测量。C/C++ 文件使用基于 Tree-sitter 的认知复杂度实现；Go 文件则以受控批次传递给 `gocognit`，并解析其 JSON 输出。当前实现尚未为 Java 和 Python 提供认知复杂度测量后端，因此针对这些语言开展实验时，需要禁用该指标或补充相应工具。

重复代码测量依据语言采用不同实现。对于 C/C++，系统向 Duplo 传入绝对路径并过滤预处理指令。系统解析其文本摘要，因为只有该输出同时包含计算重复比例所需的重复行数与过滤后总行数。默认最小重复块长度为 4 行。对于 Go，`mibk/dupl` 在语法词元表示上执行检测，默认最小克隆长度为 100 个语法词元。

所有分析异常都采用保守失败策略。可执行文件缺失、命令超时、进程异常退出或输出无法解析，都会终止本次测量并生成明确的 `analysis_failed` 结果。系统不会把缺失测量值解释为零违规，从而避免将分析工具失败误判为代码质量改善。

## 3.6 智能体角色与交互协议

### 3.6.1 编排智能体

编排智能体是一个无持久会话状态的决策角色，只在任务分派和卡死智能体评估两个决策点被调用。任务分派时，它接收当前惩罚、基线惩罚、停滞计数、逐指标惩罚分解、空闲智能体身份，以及经过压缩的产品待办列表。编排智能体可以为每个编程智能体分配一到两个问题，也可以在没有可执行问题或系统接近停滞时请求新的分析扫描。

协调器会依据产品待办列表的最新状态验证每一项分派。未知或非 `TODO` 状态的问题、分配给非空闲智能体的任务、重复分派、已经达到尝试上限的问题，以及会使不同编程智能体同时修改同一文件的分派都会被拒绝。因此，编排智能体只能提出调度建议，不能绕过确定性的并发约束。

第二个决策点在编程智能体超过单问题软时间阈值时触发。编排智能体会收到模型实际工作时间、已分配问题、是否已经编辑文件、合并门禁调用次数，以及最近一段助手日志，并据此决定继续、终止，或将指定问题判定为不可行。

在 DeepSeek 模式下，编排智能体关闭扩展思考，并通过受 JSON Schema 约束的强制工具调用返回结果。这一角色只需在有限状态快照上进行调度，不需要较长的推理轨迹，而结构化输出可以降低协议失败率。在 Anthropic 模式下，系统解析语义等价的逐行文本协议。两种情况下，原始模型响应和实际传入解析器的内容都会被保存，以便事后诊断。

### 3.6.2 分析智能体

分析智能体只检查已经被系统接受的代码。每个分析智能体运行在 detached worktree 中，并在扫描前同步到当前集成分支。这样可以避免分析结果受到某个编程智能体尚未合并的临时修改影响。

协调器向分析智能体提供当前逐指标惩罚分解，以及一个受限的本地静态分析线索页面。同一函数的 CCN、NLOC、参数个数和认知复杂度违规会被合并为一条线索，并根据假设将其降低至阈值时可消除的惩罚进行排序。这些线索不具有权威性：分析智能体必须阅读目标函数，并判断是否存在安全且保持行为不变的重构方案。经过确认的问题采用逐行 `ISSUE` 协议输出，其中包含文件、行号、严重性、问题类型、指标值和简短说明。

协调器在写入产品待办列表前验证文件是否存在、路径是否位于允许范围内，并对路径和问题类型进行规范化。随后，系统估算该问题的潜在惩罚收益并执行去重。指向不存在文件或超出范围文件的结果会被丢弃，并计入虚假问题统计。

### 3.6.3 编程智能体

每个编程智能体在隔离的特性分支中工作。一次分派最多包含两个问题，并要求按顺序逐一处理。处理某个问题前，编程智能体首先将工作树同步到当前集成分支，随后阅读目标代码及其相关依赖，实施非功能性重构并提交候选修改，最后调用合并门禁。构建与测试由门禁统一执行，编程智能体不能通过单独执行另一组命令来替代正式验收路径。

对于曾经失败的问题，编程智能体会收到有界的历史反馈，包括先前策略、门禁结果、惩罚变化以及已保存候选补丁的位置。提示要求模型不得重复已经验证失败的策略，除非新方案在设计上存在实质差异。默认情况下，同一问题在整次运行中最多被分派三次，而每次分派内部最多调用合并门禁三次。

合并成功后，编程智能体输出包含问题标识和门禁前后惩罚值的结构化 `RESULT` 行；跳过问题时则输出问题标识与原因。协调器不会直接信任上述声明，而是要求在门禁日志中找到同一编程智能体、同一问题和相同惩罚值的成功记录。若门禁已经完成合并，但模型在输出最终结果前退出，系统会使用确定性的门禁记录恢复该完成状态。

### 3.6.4 产品待办列表

持久化的产品待办列表构成问题发现与重构实施之间的接口。每个条目包含唯一标识、源代码位置、问题类型、分析说明、已解析指标值、预计改善量、影响等级、状态、负责人、分派次数和尝试历史引用。其主要状态转换为：

\[
TODO \rightarrow IN\_PROGRESS \rightarrow \{DONE,SKIPPED\}。
\]

被中断的任务可以从 `IN_PROGRESS` 返回 `TODO`。系统使用“文件路径、行号和问题类型”的三元组对所有状态的条目进行去重，包括已经完成和已经跳过的问题。保留 `SKIPPED` 条目是必要的，否则同一不可行问题可能在后续分析中被反复发现。

## 3.7 词元高效的上下文管理

优化后的系统通过保守的信息选择降低模型上下文，而不改变协调协议、质量目标或候选验收条件。具体采用三项机制。

第一，编排智能体可以看到全部活动问题和各状态的总数，但默认只接收预计影响最大的 24 个 `TODO` 问题。排名更低的待办问题，以及已完成和已跳过问题的正文仍完整保留在本地。序列化后的视图明确报告已展示和被省略的问题数量，使模型能够意识到当前提示并非完整数据集。

第二，本地静态分析线索采用分页机制。每次分析任务默认最多接收 15 条互不重叠的线索，并按预计惩罚收益排序。只有当分析进程正常退出且产生有效的终端结果时，相应线索才会被标记为已检查；失败或被截断的会话将释放其线索，以便后续重新检查。这一机制既保留了问题发现的覆盖范围，又避免每个模型会话重复执行全树扫描。

第三，协调器会跳过无法产生有效工作的模型调用。当没有空闲智能体能够处理当前状态时，系统不会调用编排智能体；分析智能体以有界批次而不是机会式补充单个空闲槽位；连续无效的编排响应或连续空分析扫描都会触发明确的终止条件。

上述优化独立于质量模型。它们不会改变指标值、产品待办语义、问题验收、编程智能体权限或合并门禁决策。因此，其影响应主要通过词元消耗、成本、分派次数和成功合并数进行评价，而不是解释为优化目标本身发生变化。

## 3.8 候选评估与集成

合并门禁是编程智能体提交进入本次运行集成分支的唯一入口。表 3 给出了按顺序执行的验证过程。

**表 3  合并门禁验证过程**

| 步骤 | 操作 | 失败后的处理 |
|---:|---|---|
| 1 | 检查工作树中是否存在未提交修改 | 不评估该候选，并要求先提交修改 |
| 2 | 将特性分支变基到当前集成分支 | 保留冲突状态，交由编程智能体解决 |
| 3 | 将修改文件与授权目标范围进行比较 | 拒绝超出范围的候选 |
| 4 | 计算候选补丁指纹并保存补丁 | 拒绝已经针对同一问题评估过的相同补丁 |
| 5 | 测量集成状态和候选状态的惩罚 | 分析异常时保守失败 |
| 6 | 要求候选惩罚严格下降 | 回退没有改善的候选 |
| 7 | 执行项目原生构建命令 | 构建失败时保留候选以供修复 |
| 8 | 执行项目原生正确性测试 | 测试失败时保留候选以供修复 |
| 9 | 快进集成分支 | 若输掉并发竞争，则重新执行完整验证 |

惩罚拒绝与构建或测试失败采用不同的处理方式。如果候选没有降低惩罚，它不满足预先定义的优化目标，因此会被回退。相反，已经降低惩罚但尚未通过构建或测试的候选可能仍然可以修复，因此系统保留该修改，使编程智能体能够根据失败信息继续调整并重新调用门禁。

门禁的每个退出路径都有明确的结果类型，用于区分惩罚拒绝、构建失败、测试失败、命令超时、进程中断、分析失败、变基冲突、快进竞争、未提交修改、范围违规、重复补丁、尝试次数耗尽和成功合并。每次完整尝试都会追加到并发安全的 JSONL 日志中，使实验能够在不解析智能体自然语言日志的情况下统计失败类型。

并发集成通过所有工作树共享的本地集成分支实现。每次验证前，候选分支都会变基到该分支，而该本地引用能够立即反映此前所有已接受修改。如果另一个编程智能体在当前候选完成变基后、执行快进前推进了集成分支，门禁将重新执行变基、静态分析、构建和测试。因此，每个被接受的提交都经过了针对其实际扩展状态的完整验证。

## 3.9 调度与停止条件

协调器以默认一秒的周期执行控制循环。每轮循环依次处理智能体消息、核对持久化门禁记录、回收已完成任务、检查词元或成本上限、检查终止条件、分派可执行任务，以及评估智能体超时。

### 3.9.1 停滞判据

设 \(\Delta_i = F(S_i)-F(S_{i+1})\) 表示一次成功合并产生的惩罚下降。默认最小有效收益为 \(\delta=10\)。停滞计数按以下规则更新：

\[
c_{i+1}=
\begin{cases}
0, & \Delta_i > \delta，\\
c_i+1, & \Delta_i \leq \delta。
\end{cases}
\]

若编程智能体达到硬超时且仍有未解决问题，停滞计数也会增加。计数达到 3 时，整次运行终止。被门禁以惩罚不下降为由拒绝的尝试会被保留为失败证据，但不会增加该多智能体系统的停滞计数，因为它没有产生新的已接受状态。

### 3.9.2 智能体时间限制

编程智能体默认具有十分钟的单问题软时间阈值和三十分钟的硬时间阈值。达到软阈值后，编排智能体判断继续工作是否合理；对同一智能体的两次评估至少间隔五分钟。达到硬阈值后，系统终止该智能体的整个进程组，将未解决问题退回 `TODO`，并增加停滞计数。

处于活动合并门禁中的时间不计入编程智能体的模型工作预算。构建、测试和整个门禁分别使用独立时限，因为项目原生验证可能合理地超过模型推理时间。分析智能体具有独立的十五分钟默认硬超时；超时后，其保留的线索页面将被释放。

### 3.9.3 其他终止条件

当初始化测量得到的惩罚已经为零、连续分析扫描没有产生可执行问题且不存在未检查线索、连续编排响应没有形成可执行分派，或者已达到配置的词元或成本分派上限且全部在途任务均已结束时，系统会停止继续调用模型。外部发送的 SIGTERM 被解释为运行时长限制，并触发受控关闭过程。

## 3.10 词元与成本测量

词元使用量来自两个数据源。同步的编排调用将服务提供方返回的使用量追加到独立 JSONL 文件；分析智能体和编程智能体的使用量则从其完整流式 JSON 日志中恢复。归一化过程分别记录未缓存输入、缓存创建、缓存读取和输出，并按角色、智能体和单次分派进行聚合。

对于 Anthropic 记录，各输入计费类别只累加一次。DeepSeek 可能同时报告提示缓存命中、未命中和提示总数；在这种格式下，命中与未命中字段替代而不是叠加提示总数。对于 Claude CLI 会话，系统优先使用最终结果事件，因为该事件包含会话级汇总。若进程在产生终端结果前被终止，则退回累计助手消息的使用量。

系统报告总词元数、统计覆盖率、轮次或调用数、有效成本，以及每次成功合并的词元消耗。Anthropic 成本可以采用 CLI 上报值；DeepSeek 成本则根据固定的逐模型价格表重新计算，因为兼容接口的历史字段可能使用具有误导性的货币名称。人民币成本使用服务提供方的原生价格计算，而不是从美元汇率换算。

可选的输入词元、输出词元、美元成本和 DeepSeek 人民币成本上限作用于分派边界。一旦系统观察到上限被触发，就不再启动新任务，但允许已经运行的智能体完成。因此，最终摘要同时记录关闭分派时的使用量和最终超出量。该语义在不丢弃已完成模型工作的同时，使预算行为保持可复现。

## 3.11 版本控制隔离与恢复

每次运行由唯一运行标识区分，并获得一个名为 `refactor/<run-id>` 的集成分支。该分支从固定的基线提交创建，目标仓库的主分支在实验过程中不会被推进。初始化前，系统拒绝存在 tracked 修改或未知 untracked 文件的仓库。系统会记录用户开始运行时处于普通分支还是 detached HEAD，并在运行结束后恢复完全相同的版本身份。

编程智能体使用特性分支工作树，分析智能体使用 detached worktree。当目标子目录能够独立构建时，系统可以采用稀疏检出；当项目原生验证依赖目标范围以外的文件时，则使用完整工作树，但不会放宽修改范围检查。

运行状态、产品待办状态、惩罚历史和分派编号均可恢复。恢复运行时，系统检出现有集成分支而不会将其移动回基线，将崩溃遗留的 `IN_PROGRESS` 条目退回 `TODO`，并重新测量当前分支。原始基线惩罚和基线指标分布保持不变，因此最终改善量覆盖崩溃前后的完整执行过程。

为了保持实验可比性，恢复过程受到带版本号的 SHA-256 配置指纹约束。当前指纹版本为 7，覆盖目标仓库、源代码范围、分析工具、阈值、权重、验证命令、服务提供方、模型、上下文选择参数、预算和价格快照。若指纹发生变化，系统要求使用新的运行标识，而不会在实验条件已经改变的情况下继续旧运行。

## 3.12 执行配置

系统包含两个采用保守失败策略的生产执行配置，具体见表 4。普通命令行参数不能覆盖这些配置中的关键范围和验证条件。

**表 4  锁定的生产执行配置**

| 属性 | FerretDB | MongoDB Query |
|---|---|---|
| 语言 | Go | C/C++ |
| 测量与修改范围 | 完整仓库 | `src/mongo/db/query` |
| 工作树内容 | 完整仓库 | 为满足 Bazel 依赖而使用完整仓库 |
| 认知复杂度工具 | gocognit | 基于 Tree-sitter 的认知复杂度工具 |
| 重复代码工具 | mibk/dupl | Duplo |
| 构建验证 | 启用竞争检测的仅编译 Go 测试过程 | 构建 `//src/mongo/db/query/...` |
| 正确性验证 | 仓库范围的短 Go 测试 | 仅测试 `//src/mongo/db/query/...` |
| 基线缓存预热 | 否 | 是，使用共享 Bazel 磁盘缓存 |

MongoDB Query 配置只直接选择 Query 子模块中的目标。Bazel 可以编译位于该子目录之外的传递依赖，但这些依赖不会被直接选为实验目标。正确性测试命令会排除集成测试和性能基准目标。在调用任何付费模型之前，系统先对 Query 基线执行构建，一方面用于尽早发现无效执行环境，另一方面用于预热后续工作树共享的 Bazel 磁盘缓存。

## 3.13 实验过程

一次独立运行按照以下步骤执行：

1. 校验实验配置、服务凭证、模型价格、分析工具和项目原生构建与测试命令；
2. 记录初始检出状态并解析不可变的基线提交；
3. 创建本次运行的集成分支和相互隔离的智能体工作树；
4. 测量基线惩罚、逐指标惩罚分解和指标分布；
5. 若配置要求，则在模型分派前执行基线构建缓存预热；
6. 进入协调循环，反复执行问题分析、任务编排、代码重构和合并门禁，直至满足终止条件；
7. 每次成功合并后重新测量完整目标范围；
8. 运行终止后核对剩余门禁证据，写入全部结果产物，删除工作树，可选推送集成分支，并恢复初始检出状态。

在重复试验中，每次运行必须使用不同的运行标识，但使用相同的基线引用和实验处理条件。需要控制的变量包括目标版本、服务提供方、编排模型和智能体模型、指标阈值与权重、智能体数量、上下文窗口参数、构建与测试命令、排除规则和停止参数。每次运行保留独立的集成分支与结果目录，前一次运行的最终状态不会成为下一次运行的基线。

完整指标处理将五项权重全部设为 1。单指标处理将目标指标设为 1，其余指标设为 0。指标消融处理将被移除指标设为 0，其余四项保持为 1。由于指标权重同时写入门禁配置和恢复指纹，因此在整次运行中保持一致。

## 3.14 数据采集与分析

每次运行都会生成结构化摘要，其中包含基线提交、实验配置、终止原因、运行时间、初始与最终惩罚、总下降量、成功合并数、指标分布、产品待办状态、失败计数、词元用量、成本和预算行为。惩罚历史文件记录基线、合并、超时和终止事件，并保存各接受状态的运行时间与逐指标惩罚分解。

门禁尝试按照明确结果类型分析，而不是依赖模型生成的自然语言。采集的失败维度包括未改善候选、构建失败、测试失败、命令超时、进程中断、变基冲突、快进竞争、分析失败、范围违规、重复补丁、尝试上限拒绝、智能体崩溃、硬超时、编排智能体主动终止、跳过问题和虚假分析问题。

主要有效性指标为相对惩罚下降：

\[
R_F = \frac{F(S_0)-F(S_n)}{F(S_0)}\times 100\%。
\]

该数值需要与五项指标的基线和最终分布以及已经清零的质量维度共同解释。效率通过总运行时间、模型总词元数、有效成本、成功合并数和每次成功合并的词元数或成本描述。可靠性则通过各类门禁结果频率、智能体崩溃、超时、跳过问题和最终终止条件描述。

只有当基线提交、目标范围、指标配置、服务提供方与模型、验证命令和相关工具版本一致时，不同运行才具有直接可比性。因外部运行时长限制而停止或词元统计覆盖不完整的运行，应单独报告或明确作为删失观测处理，而不应无说明地与自然终止的运行合并。

## 3.15 可复现性与方法边界

系统在每次运行中写入完整配置快照，并将已接受代码保存在独立运行分支中。每次智能体分派对应一个独立日志文件，既避免覆盖历史会话，也防止旧结果行在后续分派中被重复解析。原始编排响应、归一化词元事件、问题尝试历史、被拒绝的候选补丁、门禁结果和惩罚时间序列共同构成重建实验过程的证据。

默认本地验证包含 11 个相互独立的测试套件，覆盖指标计算、Git 隔离、合并门禁、并发集成、结果产物、停止规则、问题尝试策略、服务提供方隔离、词元统计、Go 认知复杂度和上下文缩减机制。这些测试使用临时仓库或替代模型调用，因此可以在不产生模型接口费用的情况下验证确定性实验基础设施。

当前方法仍存在若干限制。首先，所选目标函数只覆盖特定结构属性，不能被视为对可维护性的完整测量。其次，严格的惩罚验收会排除收益完全位于既定指标之外的修改。再次，项目原生测试可以降低语义变化风险，但不能保证完全排除行为差异。此外，当前实现不包含单智能体对照，也没有将运行性能纳入合并条件。最后，确定性基础设施测试能够验证协议和集成逻辑，但仍需要大规模重复实验来估计真实大语言模型智能体的行为表现及其方差。
